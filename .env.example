# Supabase browser read configuration. VITE_* variables are public in Vite/Netlify.
VITE_SUPABASE_URL=https://ealbeiqstgqsdjmobxly.supabase.co
VITE_SUPABASE_ANON_KEY=replace-with-readonly-anon-key

# Velz conductor/orchestrator endpoint used by dashboard triggers.
# This is public browser config for the internal MVP; do not put secrets here.
VITE_CONDUCTOR_BASE_URL=https://velz-signals-conductor-stg.blackocean-de4b65c4.westeurope.azurecontainerapps.io

# Optional Outreach backend action contract. Leave unset to keep action controls disabled/no-op
# while still showing source-backed read status. The browser calls only the internal
# backend by lead_id; it never calls Saleshandy and never sends fabricated lead context.
VITE_OUTREACH_API_BASE_URL=
VITE_OUTREACH_GENERATE_SEQUENCE_PATH=
VITE_OUTREACH_APPROVE_SEQUENCE_PATH=
VITE_OUTREACH_REJECT_SEQUENCE_PATH=
VITE_OUTREACH_LAUNCH_SALESHANDY_PATH=/orchestration/saleshandy/bulks/qa-single-lead/launch

# Backward-compatible alias for the QA-only Saleshandy launch CTA.
VITE_OUTREACH_ORCHESTRATION_BASE_URL=

# Netlify Edge Function dashboard access credentials.
# Configure these in Netlify Site settings -> Environment variables.
# Never commit real passwords or session secrets.
VELZ_DASHBOARD_USERNAME=miguel
VELZ_DASHBOARD_PASSWORD=replace-with-generated-strong-password
# Optional: lets you rotate the password without invalidating existing sessions.
VELZ_DASHBOARD_SESSION_SECRET=replace-with-generated-strong-session-secret
